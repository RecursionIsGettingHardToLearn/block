import VoteVerifier from '../../components/common/VoteVerifier';

/**
 * Panel de autoverificación del votante. Envuelve el componente compartido
 * VoteVerifier con los textos orientados al votante.
 */
export default function VoterVerify() {
  return <VoteVerifier />;
}
